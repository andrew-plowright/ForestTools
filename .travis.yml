# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

addons:
  apt:
    packages:
      - gdal-bin
      - libgdal1-dev
      - libproj-dev
      - libcurl4-gnutls-dev
      - libv8-dev
